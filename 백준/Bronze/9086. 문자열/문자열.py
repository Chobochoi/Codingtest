# 코딩테스트에서 입출력이 많지 않다면 이렇게 간단하게 작성 가능

# 테스트 케이스 개수 입력
N = int(input())

# N번 반복
for _ in range(N):
    # 문자열 입력받기
    s = input()
    
    # 첫 글자와 마지막 글자 출력
    # s[0]: 첫 번째 문자 (인덱스 0)
    # s[-1]: 마지막 문자 (음수 인덱싱으로 뒤에서 첫 번째)
    print(s[0] + s[-1])